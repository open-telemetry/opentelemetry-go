# Experimental Metric SDK Features

This package contains experimental options for the OpenTelemetry Go Metric SDK (`go.opentelemetry.io/otel/sdk/metric`).
These options are currently under development and not part of the stable SDK API.
They may be changed in backwards-incompatible ways, or removed entirely.
