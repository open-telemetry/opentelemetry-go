name: codespell
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Codespell with annotations
        run: make codespell
