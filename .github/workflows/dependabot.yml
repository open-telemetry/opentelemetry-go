name: Dependabot-Tidier
on:
  pull_request:
    types: [ labeled ]

jobs:
  mod_tidier:
    if: ${{ contains(github.event.pull_request.labels.*.name, 'dependencies') }}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: ${{ github.head_ref }}
    - uses: actions/setup-go@v2
      with:
        go-version: '^1.14.0'
    - uses: evantorrie/mott-the-tidier@477fbfa0a0598b231ea347dcdeb740811b6be5be # v1-beta on 2025-04-02, TODO: consider using a release
      id: modtidy
      with:
        gomods: '**/go.mod'
        gomodsum_only: true
    - uses: stefanzweifel/git-auto-commit-action@3ea6ae190baf489ba007f7c92608f33ce20ef04a # v4 on 2025-04-02, TODO: consider using a release
      id: autocommit
      with:
        commit_message: Auto-fix go.sum changes in dependent modules
    - name: changes
      run: |
        echo "Changes detected: ${{ steps.autocommit.outputs.changes_detected }}"
