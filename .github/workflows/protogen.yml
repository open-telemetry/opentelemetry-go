name: Proto-go-generator
on:
  pull_request:
    paths:
    - 'exporters/otlp/internal/opentelemetry-proto'

jobs:
  protogen:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - uses: actions/setup-go@v2
      with:
        go-version: '^1.14.0'
    - run: sudo apt-get -y install pax
    - run: make -f Makefile.proto protobuf
    - uses: stefanzweifel/git-auto-commit-action@3ea6ae190baf489ba007f7c92608f33ce20ef04a # v4 on 2025-04-02, TODO: consider using a release
      id: commit-changes
      with:
        commit_message: Commit changes in updated/new protobuf files
